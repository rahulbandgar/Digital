#!/bin/sh
# Maven wrapper script
set -e

MAVEN_PROJECTBASEDIR=$(pwd)
MAVEN_WRAPPER_JAR="$MAVEN_PROJECTBASEDIR/.mvn/wrapper/maven-wrapper.jar"
MAVEN_WRAPPER_PROPERTIES="$MAVEN_PROJECTBASEDIR/.mvn/wrapper/maven-wrapper.properties"

# Read wrapper URL from properties
WRAPPER_URL=$(grep wrapperUrl "$MAVEN_WRAPPER_PROPERTIES" | cut -d'=' -f2)
DISTRIBUTION_URL=$(grep distributionUrl "$MAVEN_WRAPPER_PROPERTIES" | cut -d'=' -f2)

# Download wrapper jar if not present
if [ ! -f "$MAVEN_WRAPPER_JAR" ]; then
    echo "Downloading Maven Wrapper..."
    curl -o "$MAVEN_WRAPPER_JAR" "$WRAPPER_URL" 2>/dev/null || wget -O "$MAVEN_WRAPPER_JAR" "$WRAPPER_URL" 2>/dev/null
fi

exec java -jar "$MAVEN_WRAPPER_JAR" "$DISTRIBUTION_URL" "$@"
